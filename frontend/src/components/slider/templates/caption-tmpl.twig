<div class="slider__caption">
    <div class="slider__caption-wrap j-caption">
        {% for item in items %}
            <div class="slider__caption-item" style="min-width: {{ width }}; flex-basis: {{ width }};">
                {{ item }}
            </div>
        {% endfor %}
    </div>
</div>
